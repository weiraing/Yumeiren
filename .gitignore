# build output
build*/
deploy/
*.log
*.bak
CMakeCache.txt

# user-specific
*.user
*.autosave
.vs/
.vscode/

# archives & temp
*.zip
.idea/
cmake-build-debug

dist/
# tools/ 整体不入库（2026-09-18 定案）：里面是本地验证探针与开发期辅助脚本，都属于开发工具，
# 不是产品的一部分。写 `tools/`（带斜杠）直接排除整个目录 —— 不再需要「tools/* 再 ! 放行」那套，
# 因为目录里已经没有要入库的东西了。
#
# 代价：新克隆里没有发版脚本，改版本号只能手工编辑 VERSION（它仍是唯一来源，见 README 的
# 「发版流程」）。构建不受影响 —— cmake/Version.cmake 只读 VERSION 文件，从不调用脚本。
tools/
new-build/
live2d-build/
data/
third_party/
.workbuddy-ai/
scripts

# docs/ 整体不入库（2026-09-18 定案）：里面是设计与审计文档，不是产品的一部分，
# 理由与上面的 tools/ 相同。写 `docs/`（带斜杠）直接排除整个目录。
#
# 注意这一行只管**新文件** —— 已经在库里的那批仍被跟踪，.gitignore 不会解除跟踪；
# 要把它们也移出去得另跑 `git rm -r --cached docs`，本次没做。README 里指向
# 新增文档的链接也一并撤掉了，留着就是死链。
docs/

# 测试代码用完即删、不进版本库（2026-09-18 定案）。
#
# 保留这一行是**兜底**：临时写出来的 tests/ 别被误提交。项目里现在已经没有任何
# 测试目标了 —— tests/ 源码、cmake/{ControllerTests,RenderTests,UiTests}.cmake 与对应的
# YUMEIREN_BUILD_*_TESTS 选项都已删除，回归验证靠 tools/ 下的探针（那批本就不入库）。
tests

# 软件自身缓存(<程序目录>\.cache)
.cache/
